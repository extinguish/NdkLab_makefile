APP_OPTIM := debug
APP_ABI := armeabi-v7a
APP_STL := stlport_static
APP_CPPFLAGS := -frtti -fexceptions
APP_PLATFORM := android-19
# we need to specify the location of the Android.mk explicitly
APP_BUILD_SCRIPT := Android.mk
